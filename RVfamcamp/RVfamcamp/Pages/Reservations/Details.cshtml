@page "{id:int}"
@model DetailsModel
@{
    ViewData["Title"] = "Reservation Details";
}

<h2>Reservation Details</h2>

<div class="card p-3">
    <p><strong>Check-In:</strong> @Model.Reservation.startDate.ToShortDateString()</p>
    <p><strong>Check-Out:</strong> @Model.Reservation.endDate.ToShortDateString()</p>
    <p><strong>Guests:</strong> @Model.Reservation.confirmationNumber>

</div>

<a asp-page="Edit" asp-route-id="@Model.Reservation.reservationId" class="btn btn-success mt-3">Edit</a>
<a asp-page="Index" class="btn btn-secondary mt-3">Back</a>